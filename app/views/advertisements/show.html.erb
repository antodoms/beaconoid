<!DOCTYPE html>
<html lang="en">
<head>

  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
<link href="https://fonts.googleapis.com/css?family=Luckiest+Guy|Permanent+Marker|Rubik+Mono+One" rel="stylesheet">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.0/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  <style>
  .container {
    margin-top: 10%;
    width: 400px;
      background-color: brown;


  }
  .carousel-inner > .item > img,
  .carousel-inner > .item > a > img {
      margin: auto;
      position: relative;
  }
  .carousel-inner > .item > h1 {

      font-size: 300%;
      float: right;
      
      margin-bottom: 40px;
      color: white;
      text-align: center;
      position: relative;
      font-family: 'Luckiest Guy', cursive;
  }

    .carousel-inner > #desc > h1{

      width: 400px;
      height: 450px;
      background-image: url('/assets/deli.jpg');
      background-image: linear-gradient(to bottom, rgba(0,0,0,0.3) 0%,rgba(0,0,0,0.3) 100%),  url('/assets/deli.jpg');
      background-repeat: no-repeat; 
      vertical-align: middle;
      line-height: 80px;
      color: white;
      text-align: center;
      font-family: 'Luckiest Guy', cursive;
  }

  </style>
</head>
<body>

<div class="container">
  <br>
  <div id="myCarousel" class="carousel slide" data-ride="carousel">
    <!-- Indicators -->
    <ol class="carousel-indicators">
      <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
      <li data-target="#myCarousel" data-slide-to="1"></li>

    </ol>

    <!-- Wrapper for slides -->
    <div class="carousel-inner" role="listbox">
      <div class="item active">
        <% if @advertisement.category_id == 1 %>
          <img src="<%= asset_path('freshfood') %>" alt="Chania" width="1000" height="505">
        <% end %>

        <% if @advertisement.category_id == 2 %>
          <img src="<%= asset_path('deli') %>" alt="Chania" width="400" height="05">
        <% end %>

        <h1> SALE ON <%= @advertisement.name %> </br>
        HURRY, HURRY, HURRY! PRICES SLASHED TO <%= @advertisement.price %> </h1>

      </div>

      <div class="item" id="desc">
      
        <h1> SALE ON <%= @advertisement.name %> </br>
              <%= @advertisement.description %> </br>
              AFTER DISCOUNT : <%= @advertisement.price%> </h1>

      </div>
    
    </div>

    <!-- Left and right controls -->
    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
      <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
      <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
      <span class="sr-only">Next</span>
    </a>
  </div>
</div>

</body>
</html>
