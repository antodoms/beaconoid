version: '2'

services:
  website:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - '.:/beaconoid'
    env_file:
      - '.env'

  # sidekiq:
  #   depends_on:
  #     - 'postgres'
  #     - 'redis'
  #   build: .
  #   command: sidekiq -C config/sidekiq.yml
  #   volumes:
  #     - '.:/beaconoid'
  #   env_file:
  #     - '.env'